<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.margonari.tdp2_frontend.activities.SettingsActivity">

    <com.devspark.robototextview.widget.RobotoTextView
        android:id="@+id/notifications_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextCaption"
        app:typeface="roboto_bold"
        android:text="Notificaciones"
        android:textColor="@color/secondary_text"/>

    <com.devspark.robototextview.widget.RobotoTextView
        android:id="@+id/notifications_option"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/notifications_label"
        android:layout_marginTop="10dp"
        android:text="Recibir notificaciones"
        android:textAppearance="@style/TextBody"
        app:typeface="roboto_regular"/>

    <Switch
        android:id="@+id/switch_notifications"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_below="@id/notifications_label"
        android:layout_toEndOf="@id/notifications_option"
        android:checked="true" />

</RelativeLayout>